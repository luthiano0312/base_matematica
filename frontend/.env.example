# Em desenvolvimento, o proxy do Vite (vite.config.ts) encaminha /api para o backend em http://127.0.0.1:8000.
# Para sobrescrever o destino da API (ex.: produção ou backend em outra porta), defina a URL completa:
VITE_API_BASE_URL=/api
